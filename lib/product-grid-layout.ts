/** Tight product grids: minimal gutter, capped column count so cards do not grow too wide. */
export const productGridClassName =
  'grid grid-cols-2 gap-1 sm:grid-cols-3 sm:gap-1.5 lg:grid-cols-4 lg:gap-2 xl:grid-cols-5';

/** Horizontal inset for product listing pages — keeps images close to the screen edge. */
export const productPageInsetClassName = 'px-2 sm:px-3 md:px-4';

/** Grid inside category section cards on home / categories browse. */
export const productSectionGridClassName =
  'grid grid-cols-2 gap-1 p-1.5 sm:grid-cols-3 sm:gap-1.5 sm:p-2 lg:grid-cols-4 xl:grid-cols-5';
